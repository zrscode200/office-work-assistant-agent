---
description: Update managed toolkit files in an existing workspace
---

Resolve the target directory and installed runtime. Read README.md and inspect the target before running `bootstrap/init-workspace.sh --update --runtime <runtime> <target>`. User data and customized configuration remain in place. Explain explicit legacy adoption and any needed manual hook/config comparison; do not delete originals, remove private Git history, or publish changes as an update side effect.
